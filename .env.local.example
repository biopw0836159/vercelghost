# 複製成 .env.local 並填值（本機開發用）。
# 同一組變數也要設到 Railway 專案的 Variables。
# 注意：.env.local 不要 commit 進 git（.gitignore 已擋 .env*）。

# ── 後端 open API ──
# 不設則預設 https://stats-crawler.up.railway.app
STATS_BACKEND_URL=
# open API 金鑰，帶在 x-api-key 標頭
APIKEY=

# ── 授權代理（繞過後端 ipGuard）──
# 後端會把未授權來源 302 轉去 booking.com，看起來像「後端掛了」其實是被擋。
# 兩種設法擇一：整串 PROXY，或拆成 HOST/PORT/USER/PASS。
PROXY=
PROXY_HOST=
PROXY_PORT=
PROXY_USER=
PROXY_PASS=

# ── 存取控制：IP 白名單（見專案根目錄 proxy.ts）──
# 逗號分隔，支援單一 IP 與 CIDR，例如：
#   ALLOWED_IPS=203.0.113.5, 198.51.100.0/24
# 沒設的話「一律拒絕」而不是一律放行 —— 這站看得到全站投注、盈虧、會員帳號與 IP，
# 設定沒做完之前絕不能裸奔。被拒頁面會顯示來源 IP，方便把自己加進來。
# 本機 next dev 會自動豁免（NODE_ENV=development），所以這裡可以留空。
ALLOWED_IPS=
