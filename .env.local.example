# 複製成 .env.local 並填值（本機開發 + 跑 seed 用）。
# 同樣三個變數也要設到 Vercel 專案的 Environment Variables。
# 注意：.env.local 不要 commit 進 git。

# 新 Supabase 專案（Project Settings → API）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=        # 用 service_role key，不是 anon key

# 自簽 JWT 用的密鑰（隨機長字串，例如 openssl rand -hex 32）
JWT_SECRET=
